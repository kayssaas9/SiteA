---
name: Auth logo spacing
description: The authentication navigation should match the landing page logo treatment.
---

The sign-in and sign-up headers use the same close A-to-name spacing as the landing page, while keeping the full `stracrea` text visible after the mark.

**Why:** The user explicitly confirmed that the landing-page alignment is the desired visual reference.

**How to apply:** Reuse the landing logo's typography and negative spacing values when changing the authentication header; do not hide the initial “s” of `stracrea`.